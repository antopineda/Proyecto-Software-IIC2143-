<h1>Bienvenido al chat del curso!</h1>
<div id="single_room">
  <h4 class="text-center"> <%= @room.name %> </h4>
  <%= turbo_stream_from @room %>
  <div id="messages" data-controller="scroll">
    <%= render @messages %>
  </div>
  <%= render "layouts/new_message_form" %>
</div>